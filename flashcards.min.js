"use strict";(()=>{let currentCardIndex=0;const[flipCard]=document.getElementsByClassName("flip-card"),[innerFlipCard]=document.getElementsByClassName("flip-card-inner"),studyData=[["Numerator","Top Number Of A Fraction"],["Denomonator","Bottom Number Of A Fraction"]],order=[],randomIntFromInterval=(min,max)=>Math.floor(Math.random()*(max-min+1)+min);function setCardData(){[document.getElementById("term").innerText]=studyData[order[currentCardIndex]],[,document.getElementById("definition").innerText]=studyData[order[currentCardIndex]]}function generateOrder(){if(order.length!=studyData.length){let nextNumber=randomIntFromInterval(0,studyData.length-1);for(;order.includes(nextNumber);)nextNumber=randomIntFromInterval(0,studyData.length-1);order.push(nextNumber),generateOrder()}}function nextCard(){currentCardIndex<studyData.length-1&&(currentCardIndex++,setCardData())}function previousCard(){currentCardIndex>0&&(currentCardIndex--,setCardData())}flipCard.addEventListener("click",()=>{innerFlipCard.classList.contains("flip-card-flip")?innerFlipCard.classList.remove("flip-card-flip"):innerFlipCard.classList.add("flip-card-flip")}),generateOrder(),setCardData(),document.getElementById("forwardButton").addEventListener("click",nextCard),document.getElementById("backButton").addEventListener("click",previousCard)})();